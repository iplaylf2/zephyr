model User {
  createdAt    DateTime
  expiredAt    DateTime
  id           Int      @id @default(autoincrement())
  lastActiveAt DateTime
  name         String
  updatedAt    DateTime

  @@map("users")
}
