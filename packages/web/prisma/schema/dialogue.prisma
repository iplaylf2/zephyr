model Dialogue {
    conversation Int      @id
    createdAt    DateTime
    expiredAt    DateTime
    initiator    Int
    participant  Int

    @@unique([initiator, participant])
    @@unique([participant, initiator])
    @@index([expiredAt])
    @@map("dialogues")
}
