model Dialogue {
    conversationId Int      @id
    expiredAt      DateTime
    initiator      Int
    participant    Int

    @@unique([initiator, participant])
    @@unique([participant, initiator])
    @@index([expiredAt])
    @@map("dialogues")
}
